# 2026-08-02 工作日志

## AI Builders Digest 日报（自动化 10:00 执行）

- 内容获取：13 Builders · 21 Tweets · 1 Podcast · 0 Blog
- `feedGeneratedAt = 2026-08-01T07:07:44Z`（滞后 1 天的快照，非冻结；与 8/1 的 07:16 快照不同源，内容已刷新）
- `prepare-digest.js` 首次运行即成功（109KB），未触发健壮兜底脚本
- **Blog 处理**：feed 里唯一 blog 是 "Claude Code now supports artifacts"（Jun 18），7/8 已收录 → 判定为旧条目，不重复计入，Blog 计 0
- **本期主线**：harness / agent loop 高度收敛
  - Aaron Levie：harness 是仅次于模型能力的最重要变量；任务从百万级 token 到亿级 token，harness 从无关紧要变成关键杠杆
  - Guillermo Rauch：Issue → Agent → PR → Release 将成常态，维护者的工作是「打磨那个循环」而非写代码；AI Gateway 定位为按 key/团队设预算 + failover + 可观测
  - Nan Yu（Linear）：同一循环的实测数据——30% 的 bug 能跑完整链路；要求 agent 用 Datadog/Sentry MCP 取证、把握不大不要动手（否则烧 token）、缺证据就在 issue 下留言要复现步骤
  - Garry Tan：YC 自用 harness 开源
  - Peter Steinberger：5.5 之后模型不再被打断搞混，排队机制可以不要了
  - Swyx：仍在用 /loop 和 /goal，认为弃用者放弃太早；"vibe coding" 贬义已消失
  - Zara Zhang：Anthropic 65% 的 PR 由 Claude Tag 提出；agent 界面半年内从终端 → 桌面端 → 协作工具迁移
- **Podcast**：Unsupervised Learning Ep 92 — Igor Babushkin（DeepMind StarCraft/AlphaCode → OpenAI 早期推理 → xAI 联创 → 新公司 River）
  - 核心论点：闭源厂商两头被夹——模型做太好可能不被允许发布，开源每月逼近
  - 训练边际递减（不可能拿 GPU 铺满地球），已接近该点
  - 企业该自训：领域纵深比 OpenAI/Anthropic 深；否则等于把差异化根基送人
  - River 三个赌注：RL/微调 API、按个体个性化（推荐系统 × agent 混合体，端到端训练、奖励函数是个人幸福感）、本地硬件跑前沿模型（控制权 + 低延迟开语音视频 + 隐私）
- 生成方式：Python 脚本 `/tmp/gen_digest_0802.py`（双语 remix + URL 从 JSON 动态取 + 自检断言）
- 校验：21 条 x.com 链接全部命中 JSON 真实字段（0 伪造）、签名存在、无未解析占位符
- 部署：✅ HTTP 200（轮询第 2 次命中），commit `d7399db`
- 文件：`2026/08/02/AI_Builders_Digest_2026-08-02.html`

# 分析经验库

## 场景：AI Builders Digest 日报生成 + GitHub Pages 部署

**日期**：2026-06-15  
**来源任务**：Automation — AI Builders Digest 日报  
**标签**：`follow-builders` `html-generation` `github-pages` `bilingual-digest`

---

### ✅ 成功打法

**步骤概要**：
1. 调用 `follow-builders` skill 的 `prepare-digest.js` 获取 JSON 内容
2. 用 Node.js 脚本生成双语 HTML（参考前一天的 HTML 样式）
3. 复制 HTML 到 `ai-digest-site` 仓库对应日期目录
4. 更新 `index.html` 的 `ISSUES` 对象（追加新日期条目）
5. `git add` + `git commit` + `git push`
6. 用 `curl` 验证部署成功（HTTP 200）

**关键细节**：
- JSON 输出可能很大，用 `head -c 50000` 会截断；应保存到文件再读取
- HTML 样式严格参考前一天文件（`#faf9f6` 背景，`accent #c2410c`，Inter + Noto Serif SC + JetBrains Mono）
- `index.html` 的 `ISSUES` 对象按日期倒序排列，新条目插到最前面
- 部署验证用 `curl -s -o /dev/null -w "%{http_code}" <url>` 检查 HTTP 200

**踩坑记录**：
- `prepare-digest.js` 的 JSON 输出含完整推文内容和 Podcast 文字稿，需合理截断或精简后放入 HTML
- Bash 命令写入 HTML 时需注意 heredoc 中的变量插值和引号转义
- 如果 shell 没有写入 Desktop 的权限，用 `mkdir -p` + `cp` 可能失败；需检查 sandbox 权限

---

## 场景：follow-builders skill 内容获取

**日期**：2026-06-15  
**来源任务**：AI Builders Digest 日报自动化  
**标签**：`follow-builders` `content-fetch` `json-parsing`

---

### ✅ 成功打法

**调用方式**：
```bash
cd /Users/workingchloe/.workbuddy/skills/follow-builders/scripts
node prepare-digest.js > /tmp/fb-digest-YYYY-MM-DD.json 2>&1
```

**输出结构**：
- `status`: "ok"
- `config`: 用户偏好（language, frequency, delivery）
- `podcasts`: [{ name, title, url, publishedAt, transcript }]
- `x`: [{ name, bio, tweets: [{ text, url, createdAt }] }]
- `stats`: { podcastEpisodes, xBuilders, totalTweets, blogPosts }
- `prompts`: { summarize_podcast, summarize_tweets, translate, digest_intro }

**注意事项**：
- `language` 决定输出语言：`en` / `zh` / `bilingual`
- 如果 `stats.podcastEpisodes` 和 `stats.xBuilders` 都为 0，说明当天没有新内容
- 推文 URL 必须从 JSON 的 `url` 字段获取，不能自己构造
- Podcast 的 `url` 是频道页，具体视频 URL 需在 transcript 中或在 skill 输出中另行获取

---

## 场景：AI Builders Digest HTML 样式规范

**日期**：2026-06-15  
**来源任务**：生成日报 HTML  
**标签**：`html-styling` `ai-builders-digest` `css`

---

### ✅ 样式参数

| 元素 | 规范 |
|------|------|
| 背景色 | `#faf9f6` |
| 强调色 | `#c2410c` |
| 强调色浅 | `#fef3c7` |
| 墨水色 | `#14161a` |
| 墨水浅 | `#3a3d44` |
| 线条色 | `#e8e6df` |
| 最大宽度 | `760px` |
| 字体 | Inter（英文）、Noto Serif SC（中文）、JetBrains Mono（代码/URL）|
| 布局 | sticky topbar、stats strip、builder cards、podcast card、takeaway、footer |
| 文件末尾 | `<!-- by chloevchen -->` |

**HTML 结构关键点**：
- `summary-en`：英文摘要，`.summary-zh`：中文摘要（带左边框 accent 色）
- 每个 builder 的 URL 放在 `.urls` div 中，用 JetBrains Mono 字体
- Podcast 用 `.podcast` card 样式，`.takeaway` 用渐变背景
- 移动端 `@media (max-width: 640px)` 需适配 padding 和字体大小

---

## 场景：follow-builders 中央 feed 冻结 → 连续日内容重复

**日期**：2026-07-03  
**来源任务**：Automation — AI Builders Digest 日报  
**标签**：`follow-builders` `feed-freshness` `dedup` `data-quality`

---

### ⚠️ 观察（踩坑预警）

- `prepare-digest.js` 从中央 feed 拉取内容，feed 含 `feedGeneratedAt` 字段。
- 若连续两日 `feedGeneratedAt` 不变 + 全部 tweet `createdAt` 停留在上一天，则当日 digest 与昨日**完全相同**（feed 未刷新）。
- 实测 2026-07-03 的 `feedGeneratedAt = 2026-07-02T07:27Z`，tweets 均在 7/1-7/2、Podcast 7/1，与 7/2 同源。
- **处置**：仍按既定流水线生成并部署（stats 检查通过），但应（1）在自动化记忆中如实标注重复；（2）将该日作为"同一快照的更完整版本"处理——补齐前一日漏渲染的 builder（7/3 渲染 17 位、7/2 仅 12 位）；（3）提示用户决定是否保留该条目，避免污染日历 streak 与"每日覆盖"的可信度。
- **判定规则**：部署前先比对 `feedGeneratedAt` 与前一日；若相同则标记"stale snapshot"，由用户决定是否落库。

---

## 场景：follow-builders prepare-digest.js 远程 fetch 间歇性失败（000 / "fetch failed"）

**日期**：2026-07-12
**来源任务**：Automation — AI Builders Digest 日报
**标签**：`follow-builders` `fetch-flaky` `retry` `robust-fetch` `network`

### ⚠️ 踩坑

- `prepare-digest.js` 对 3 个 feed + 5 个 prompt 用 `Promise.all([fetchJSON(...), ...])` 一次性拉取，**零重试**。
- 当任一远程 URL（实测 `feed-blogs.json`、`prompts/translate.md`）出现连接级 `fetch failed`（curl 探测返回 `000`，非 404）时，整次 `main()` 被 reject，stderr 仅 `{"status":"error","message":"fetch failed"}`，stdout 为空 → 获取连败 2 次。
- 同会话内 `curl github.com`=200、`node fetch google`=200，说明**不是整体断网**，而是 raw.githubusercontent.com 部分资源间歇性握手失败（DNS/TLS/限流随机掉线）。

### ✅ 打法（已验证）

- 自写健壮抓取脚本（参考 `/tmp/fb-prepare-robust.mjs`），核心：**每个 URL 单独重试**（6 次、指数退避 + 抖动），不共用一个 Promise.all。
- feed 兜底顺序：远程(重试) → 技能目录内置 `feed-*.json`（仅当远程彻底不可达，且**必须打 STALE 标记告知用户，绝不冒充"今日"内容**）→ 否则报错。
- prompt 兜底顺序：用户自定义 > 远程(重试) > 技能内置。
- 输出 schema 与官方 `prepare-digest.js` 完全一致，下游 HTML 生成脚本无需改动。
- **铁律**：技能内置 `feed-*.json` 是 4/27 旧快照，绝不能作为"当天 digest"内容来源；远程失败只能重试，不能拿旧数据顶替。
- 验证：重试脚本一次跑通，103KB 合法 JSON，`feedGeneratedAt` 为当日（2026-07-12T01:19Z）。

---

## 场景：feed 里的 blog 长期滞留旧条目 → 需按"是否已收录"去重，而非照抄 stats

**日期**：2026-08-02
**来源任务**：Automation — AI Builders Digest 日报
**标签**：`follow-builders` `blog-dedup` `stats口径` `数据质量`

### ⚠️ 踩坑

- `stats.blogPosts` 只统计 feed 里 blog 数组的长度，**不判断新鲜度**。
- 实测 2026-08-02 的 blog 唯一条目是 `Claude Code now supports artifacts`（publishedAt = **Jun 18, 2026**），该条目在 **7/8 的 digest 已收录过**；feed 侧的 blog 源刷新频率远低于 X/Podcast，会长期滞留同一篇。
- 若直接把 `stats.blogPosts=1` 写进 stats strip 和 index meta，会让读者以为当天有新博客，实际是一个半月前的旧文。

### ✅ 打法

1. **blog 必须按 `publishedAt` + 历史收录记录二次判定**，不能直接用 `stats.blogPosts`：
   - `publishedAt` 距当日超过 ~7 天 → 高度可疑
   - 去 automation memory.md 里 grep 该标题是否已出现过 → 命中则判为旧条目
2. 判为旧条目时：**正文不渲染该 blog 卡片，stats 与 index meta 的 Blog 一律计 0**，并在 automation memory 里写明判定理由（哪天已收录）。
3. 同样的判定逻辑适用于 Podcast——但实测 Podcast 刷新正常，主要风险在 blog。
4. **stats 口径铁律**：HTML stats strip 的四个数字必须与 index.html 的 `meta` 字符串完全一致，且都以"实际渲染进正文的内容"为准，不以 JSON 的 `stats` 原值为准。

### 🔁 2026-08-03 复现验证：同一 blog 已滞留 3 个月，去重规则必须查全量历史

- 8/3 feed 的唯一 blog 仍是 `How we contain Claude across products`，且**连 `publishedAt` 字段都是 `None`**——比 8/2 那次更难判新鲜度。
- 仅 grep automation memory.md 只命中 1 处（6/30），**容易误判为「只收录过一次、可以再发」**。
- **升级后的判定命令**（必须同时查两处）：
  ```bash
  # ① 查 automation 记忆
  grep -c "<blog 标题>" .workbuddy/automations/<id>/memory.md
  # ② 查已发布 HTML 全量（更权威）
  grep -rl "<blog url slug>" 2026/
  ```
  实测 ② 命中 `2026/06/05`、`2026/06/21`、`2026/06/30` 三份 —— 该 blog 已滞留近两个月。
- **铁律补充**：`publishedAt` 为 `None` 时不能默认「新条目」，必须走 ② 的全量 HTML grep；命中即计 0。

---

### 📌 X/Twitter 卡片数与 tweet 数的口径

- 本期 13 位 builder 中，有几位只挑选了部分推文（如 Sam Altman 3 条只取 1 条、Nikunj 3 条只取 1 条——其余是纯链接/生活向内容无实质观点）。
- 因此 **Tweets 计数必须按"实际渲染的 URL 条数"统计（21），不能用 `stats.totalTweets`（31）**。生成脚本里用 `sum(len(tweets) if idxs is None else len(idxs))` 动态算，避免两处对不上。

---

## 场景：GitHub Pages 部署后 URL 短暂 404（Actions 构建延迟）

**日期**：2026-08-04
**来源任务**：Automation — AI Builders Digest 日报
**标签**：`github-pages` `deploy-wait` `verification` `gh-run`

### ⚠️ 踩坑

- `git push` 成功后立刻 `curl` 验证页面，返回 **HTTP 404**（返回约 9KB 的 GitHub 404 页，非 digest）。
- 原因：仓库用 `.github/workflows/deploy.yml`（actions/deploy-pages）在 push 到 main 时整库构建部署，run 约需 19–22s 才 `completed`。

### ✅ 打法

- push 后**不要立即 curl 判失败**；先 `gh run list --limit 1` 确认状态变 `completed/success`（通常 <30s），再 curl 验证 HTTP 200。
- 修正计数后二次 push 同理：第二次 push 的 run 仍 `in_progress` 时线上还是上一次产物，等 completed 再验。
- 验证命令：`code=$(curl -s -o /tmp/p.html -w "%{http_code}" <url>)`；digest 页 stat-num 与 label 分行，grep 用 `<div class="stat-num">21</div>` 而非连续串 "21 Tweets"。
- `gh` CLI 在本机可用且已认证，可直接 `gh run list` 监控部署，比盲等 curl 更稳。

---

## 场景：stats 口径两条规则冲突时的最终裁决（2026-09-08）

**日期**：2026-09-08
**来源任务**：Automation — AI Builders Digest 日报
**标签**：`stats口径` `规则冲突` `rendered-vs-feed` `数据质量`

### ⚠️ 冲突

项目经验库里存在两条互不一致的 stats 规则：
- 2026-08-02 铁律：stats strip 与 index meta **必须一致，且以"实际渲染进正文的内容"为准**，不以 JSON `stats` 原值为准（该条针对 blog 长期滞留旧条目）。
- 09-03 ~ 09-07 连续实践：stats 报 **feed 总数**，同时按 skill 的 summarize_tweets 规则跳过一部分薄内容推文（如纯梗图 / 政治 / engagement fluff）→ 结果 stats 数字 > 正文实际渲染数，日历上出现系统性虚高。

### ✅ 裁决（本期起执行）

**让"渲染数"去追平"feed 总数"，而不是反过来把 stats 改小。**

具体做法：对 feed 里的每一条内容都渲染，薄内容（调侃、一句话反应、纯链接）不删除，而是**各配一句克制的定性说明**（如"论证层面很薄，但如实反映了……的随意程度"），让读者自行判断价值。这样：
- 渲染数 == feed 总数 == stats strip == index meta，四处数字必然一致，无需人工对账；
- 不违反 skill 的"绝不编造"规则（没有替薄内容虚构观点）；
- 也不违反 08-02 铁律（统计的就是渲染出来的东西）。

**判断顺序**：① feed 内容是否陈旧/已收录（走既有去重规则）→ 剔除后得到 feed 有效总数；② 有效总数内全部渲染；③ stats/index meta 填该总数。
**只有一种情况可以把 stats 改小**：某条内容被去重规则判为"旧条目/已收录"而剔除（如 08-02、08-03 的 blog）。编辑性地跳过薄内容**不属于**这种情况。

**根因提示**：把"编辑判断（要不要写深写浅）"和"统计口径（算不算一条）"混为一谈，是这类不一致的根源。前者影响措辞，后者影响数字，两者必须解耦。

---

## 场景：Podcast 去重不能只看 publishedAt，要做内容级 grep（2026-09-08）

**日期**：2026-09-08
**来源任务**：Automation — AI Builders Digest 日报
**标签**：`follow-builders` `podcast-dedup` `publishedAt` `数据质量`

### ⚠️ 观察

- 既有经验（08-02）记录的是"Podcast 刷新正常，主要风险在 blog"，但本期 podcast 的 `publishedAt` 是 **2026-08-27**，距 digest 日 12 天——单纯按"超过 7 天即可疑"会被误杀。
- 该期（No Priors × EON 创始人）在 `2026/` 全量 HTML 中 grep `Eon` / `Ofir` / `Gonen` **均无命中**，属"旧发布、新进 feed"，此前从未收录。

### ✅ 打法

1. **podcast 判重以内容级 grep 为准，publishedAt 只作提示、不作判据**：去 `2026/` 下 grep 嘉宾姓名 + 公司名 + 标题关键词（如 `grep -rl "Ofir" 2026/`）；命中即判重复计 0，未命中则**照常收录**，即便 publishedAt 陈旧。
2. 注意 ASR 转写会把人名读错（本期文字稿里 Ofir → "Ofer Oerlech"、Gonen → "Goenen Stein"），**grep 必须用 JSON 的 `title` 字段里的正确拼写**，不要用 transcript 里的读音。
3. 收录陈旧 publishedAt 的条目时，在当日日志与 automation memory 里注明"旧发布新收录"，避免日后被误判为重复。

---

## 场景：0 podcast / 0 blog 期如何保住 takeaway 结构（2026-09-10）

**日期**：2026-09-10
**来源任务**：Automation — AI Builders Digest 日报
**标签**：`template` `zero-content-section` `takeaway` `no-fabrication`

### ⚠️ 问题

- 稳定模板（2026-08-31 起）把 `.takeaway` 块**嵌在 `.podcast` 卡片内部**，与 podcast 的 EN/ZH 正文强绑定。
- 当 feed 返回 `podcasts: []`（本期 15 builders / 33 tweets / 0 podcast / 0 blog）时，若整段删掉 PODCAST section，页面会**丢失 takeaway 这一唯一的编辑总结元素**，只剩一串 builder 卡片，读起来没有"今日主线"。

### ✅ 打法

1. **不为了填模板硬造播客段落**——skill 的"绝不编造"规则优先于版式完整性。
2. **把 takeaway 提成独立的 `<section class="section">`**：
   - `.section-head`：`section-icon` 🧭 + `section-title` "Takeaway" + `section-meta` 写 **"No Podcast Today"**（如实告知为何没有播客，不藏）
   - 内部保留**两条 `.takeaway`**（EN + `.zh`），沿用原有渐变样式与 `takeaway-label`（"The Takeaway" / "核心结论"）
3. 内容改为**当日 X 内容的主线归纳**，只引用已渲染进正文的素材，不引入新事实。
4. stats strip 的 Podcast / Blog 如实填 0，index meta 同步。

### 🔁 复用规则

- 有 podcast → 沿用旧结构（takeaway 在 podcast 卡内）
- 无 podcast 但有 X 内容 → 独立 Takeaway section + `section-meta: "No Podcast Today"`
- 有 blog 时同样按需增删 OFFICIAL BLOGS 段落（见 09-06 / 09-07 条目）

---

## 场景：跳过一天后不要补发，feed 快照本就滞后约 1 天（2026-09-10）

**日期**：2026-09-10
**来源任务**：Automation — AI Builders Digest 日报
**标签**：`feed-lag` `missed-day` `no-backfill` `calendar-integrity`

### ⚠️ 观察

- 2026-09-09 未生成（日历上 `2026/09/09/` 目录不存在），09-10 触发时拿到的 feed 是 **2026-09-09T06:41Z** 的快照。
- 历史对照：09-08 那期用的 feed 是 2026-09-07T06:50Z —— 说明 feed 生成时刻比 digest 日**滞后约 1 天是常态**，不是"陈旧告警"。

### ✅ 打法

1. 跳过 1 天后，**次日那期直接把该快照当当天内容发布**，不回头补发前一天的条目。
   - 理由：补发会用到同一份 feed，等于同一批内容在日历上出现两次，污染 streak 与"每日覆盖"的可信度。
2. 只在 automation memory 与当日日志里**如实标注空档 + 该期 feed 的 `feedGeneratedAt`**，供日后判重时参考。
3. 与 07-03 的"stale snapshot"规则区分开：
   - **连续两日 `feedGeneratedAt` 完全相同** → 判为 feed 冻结（07-03 规则）
   - **滞后 1 天但快照前进了一天** → 正常，照发（本条）
4. 判重时（blog / podcast）用内容级 grep 而非日期，见 09-08 条目。

### 🔁 2026-09-13 复现：跳过一天会**永久丢掉一份快照**

- 09-12 未运行，09-13 触发时 feed 是 `2026-09-12T06:36Z`，比 09-11 那期用的 `2026-09-10T06:40Z` 前进了 **2 天**。
- 与 09-10 那次（跳 1 天、快照只前进 1 天、内容没丢）**不同**：本次 `2026-09-11T06:xx` 那份快照随着 09-12 被跳过而**永久无法追回**——feed 只保留最新一份。
- **处置不变**：不补发被跳过的那天，把最新快照作为当天一期发布；但在 automation memory 与当日日志里注明「丢档：XX 日快照未采集」，供日后判断 streak 缺口时参考。
- **推论**：这个 feed 没有历史回放接口，任何一次未触发都会造成内容缺口，无法通过次日补跑找回。

---

## 场景：`gh run list --workflow` 用错名称会静默返回空，导致轮询死循环（2026-09-11）

**日期**：2026-09-11
**来源任务**：Automation — AI Builders Digest 日报
**标签**：`gh-cli` `github-actions` `部署验证` `死循环`

### ⚠️ 事故

- 按既有经验"先 `gh run list` 轮询到 completed/success 再 curl"，本期写成：
  `gh run list --workflow "pages-build-deployment" --limit 1 --json status,conclusion`
- 该 workflow 名在本仓库**不存在** → 返回空数组 → `-q '.[0].status'` 输出空串 → `case` 三个分支都不匹配 → `sleep 10` 后重试，**跑满 20 次 / 25 分钟**，后台任务被系统自动挂起才停下。

### ✅ 正确做法

1. **不要指定 `--workflow`**，直接看最近一条：
   ```bash
   gh run list --limit 1 --json displayTitle,status,conclusion,databaseId
   ```
2. **本仓库的部署 workflow `displayTitle` = commit message**（如 "AI Builders Digest 2026-09-11"），不是 GitHub 默认的 `pages-build-deployment`。仓库里是自定义 Actions workflow，不是 GitHub 原生的 Pages 构建。
3. **轮询必须带兜底退出**：`for i in $(seq 1 N)` + 空值也计入尝试次数，宁可超时也不要静默空转。更好的写法是用 `gh run watch <id>`，自带超时。
4. 顺序不变：Actions `completed/success` → 再 `curl -I` 判 200。本期确认该顺序有效，子路径**首查即 200**（未遇 CDN 边缘 404）。

### 一句话记住

**轮询前先确认查询能返回东西。** 空结果不是"还没开始"，是"查错了"。

---

## 场景：podcast URL 两种形态 + 有/无 podcast 的结构二选一（2026-09-13）

**日期**：2026-09-13
**来源任务**：Automation — AI Builders Digest 日报
**标签**：`follow-builders` `podcast-url` `template-branch` `no-fabrication`

### ✅ 打法

1. **podcast `url` 字段有两种形态，都出现过**：
   - 具体视频：`https://www.youtube.com/watch?v=<id>`（09-07、09-13）
   - 只有频道页：`https://www.youtube.com/@<channel>`（09-05、09-08）
   - 给什么用什么。**有具体链接就用具体链接；只有频道页就用频道页**，不联网补抓、不按标题推测 video id。
2. **版式按当日 feed 二选一，两条路径都已跑通**：
   - 有 podcast → takeaway 嵌回 `.podcast` 卡内（原始稳定结构）
   - 无 podcast → 独立 `<section class="section">` 承载 Takeaway + `section-meta: "No Podcast Today"`（见 09-10 条目）
   - 不要在 0 podcast 的日子为了填模板硬造播客段落；也不要在有播客的日子沿用了独立 section 的写法。
3. 播客摘要优先抽**可核对的数字与事实**（收入占比、交易额门槛、增速、临床适应症），再补观点；ASR 转写的人名/公司名会读错，引用时用 JSON `title` 字段的正确拼写。

## 2026-09-14 · digest 去重后 stats 归零的表达处理
**场景**：AI Builders Digest 日更时，某一类内容（播客/博客）全部被判为历史已收录，stats 显示 0。
**问题**：stats 报 0 会让读者误以为抓取失败，而只写一句"已去重"又不留证据。
**做法**：在正文新增「已收录 / Already Covered」区块（复用卡片样式 + 一条 dashed 边框的 `.dedup`），逐条列出：来源类型 + 标题 + **回链到原期页面** + 原始链接；同时在 stats strip 下方加一行灰色小字说明"按去重规则不计入统计，仅列出链接"。
**判定规则**：去重看内容不看发布日期——决定性证据是 `2026/` 全量 URL grep 是否命中历史页面；命中即剔除，未命中即使 publishedAt 陈旧也照常计入（与 09-08 的"旧发布新收录"互为反面）。
**来源任务**：automation-1777275206372 / 2026-09-14 AI Builders Digest
**标签**：#digest #去重 #stats口径 #信息呈现

## 2026-09-15 · 播客槽位连续两日回吐旧内容 + 验证必须用 GET
**场景**：连续两期（09-14、09-15）的 podcast 都被判为历史已收录（09-14 命中 09-05、09-15 命中 09-06）。
**观察**：feed 的 podcast 槽位**只有一个**，且在重复回吐较早的集数——`publishedAt` 分别是 2026-09-03 与 2026-09-03，远早于当日；X 部分每天都是新的，播客不是。
**处置**：
1. 判据不变——`grep -rl "<嘉宾名>" 2026/` 命中即剔除，stats 归 0，并进「已收录 / Already Covered」区块回链原期。本期只 1 条，区块 `section-meta` 写 "1 Item · Deduped"。
2. 区块位置固定在 Takeaway 之后、footer 之前；note-line 的措辞跟着条目数走（1 期播客 / N 篇 blog）。
3. 不要因为"连续两天播客都是 0"就怀疑抓取坏了——先 grep 确认是不是去重，再下结论。
**验证坑（新）**：`curl -I` 只落响应头（702 bytes），grep 正文恒 0 命中会误判部署失败。校验正文必须用 GET 并用 `%{size_download}` 对齐本地 `wc -c`（本期 35013 == 35013）。
**来源任务**：automation-1777275206372 / 2026-09-15 AI Builders Digest
**标签**：#digest #去重 #podcast #curl #部署验证

## 2026-09-16 · 一 builder 一卡：卡片数必须等于 Builders 数
**场景**：某位 builder 当天有 2 条以上相互独立、都值得展开的长推文（本期 Aaron Levie 两条：agent 任务量 100X + 治理两难）。
**踩坑**：直觉做法是按推文拆成两张卡片（"Aaron Levie" / "Aaron Levie · 治理篇"）→ 结果卡片 13 张 vs stats strip 的 12 Builders，且同一条 URL 在两张卡里各出现一次，导致"唯一推文 URL 计数"（用来校验 stats 的 Tweets 数）对不上。
**做法**：**卡片数 = builder 数，不是推文数**。多推文用卡内多组 EN/ZH 段落承载（第 1 条 EN→ZH，第 2 条 EN→ZH），段落顺序按推文时间或逻辑排，URL 集中在该卡末尾 `.urls`，每条只列一次。
**校验顺序（生成后固定跑）**：
1. `grep -c '<article class="builder">'` == stats 的 Builders
2. `grep -o 'https://x.com/.*/status/[0-9]*' | sort -u | wc -l` == stats 的 Tweets
3. stats strip 四个数字 == index.html 的 `meta` 字符串
**来源任务**：automation-1777275206372 / 2026-09-16 AI Builders Digest
**标签**：#digest #stats口径 #模板 #一致性校验

## 2026-09-17 · 推文 URL 计数校验的 grep 贪婪陷阱
**场景**：生成后按既定校验清单跑「唯一推文 URL 数 == stats 的 Tweets」。
**踩坑**：沿用旧命令 `grep -o 'https://x.com/.*/status/[0-9]*' file | sort -u | wc -l`，结果输出 **14**（= 卡片数）而不是 27（= 推文数），一度以为渲染漏了一半。
**根因**：`.urls` 里所有 `<a>` 都在**同一行**，贪婪 `.*` 会从该行第一个 `https://x.com/` 一路吞到最后一个 `/status/<id>`，整行只产出 **1 个匹配**。所以计数实际等于"有多少个 `.urls` 行"，与推文数无关——当每个 builder 只有 1 条推文时数字会碰巧对上（所以之前几期没暴露）。
**正确写法**：用字符类限定路径段，禁止跨段贪婪：
```bash
grep -o 'https://x\.com/[a-zA-Z0-9_]*/status/[0-9]\+' file | sort -u | wc -l
```
**更强的校验**：把抽出的 ID 与 feed 原始 ID 列表 `diff`（`grep -o ... | sed 's#.*/status/##' | sort -u` vs feed 的 `t.url` 抽取结果），两者完全一致才是真的没问题。
**来源任务**：automation-1777275206372 / 2026-09-17 AI Builders Digest
**标签**：#digest #校验 #grep #shell

## 2026-09-17 · podcast 的 url 字段有第三种形态：playlist
**场景**：podcast 条目给的是 `https://www.youtube.com/playlist?list=<id>`（频道合集页），既不是 09-07/09-13 的具体 `watch?v=`，也不是 09-05/09-08 的 `@channel` 频道页。
**做法**：三形态一视同仁——**feed 给什么就用什么**，不联网补抓、不按标题推测 video id。
**判重的坑**：playlist id 属于"频道级"资源，会在 `2026/` 历史页面里大量命中（本期 `PLOhHNjZItNn...` 命中 7 期），**不能作为去重证据**。判重必须回到 09-08 定的内容级规则：用 JSON `title` 里的**标题关键词**（如 "Reinventing Yourself"）去 grep，不要用 URL 里的 ID。
**来源任务**：automation-1777275206372 / 2026-09-17 AI Builders Digest
**标签**：#digest #去重 #podcast #no-fabrication

## 2026-09-16 · feed 的 podcast 与 blog 槽位在循环回吐旧内容（连续三日判重）
**观察**：09-14 命中 09-05、09-15 命中 09-06、09-16 命中 09-04（podcast）+ 09-07（4 篇 blog）。X 侧每天都全新，podcast/blog 不是。
**判断**：feed 的 podcast 槽位只有 1 个、blog 槽位是固定几篇，都在循环回吐；**连续多日 podcast = 0 不等于抓取失败**。
**做法**：判据不变——`grep -rl "<标题 / 嘉宾名>" 2026/`，命中即剔除并进「已收录 / Already Covered」区块；同日多类内容全去重时，`section-meta` 写 "N Items · Deduped"，note-line 写明"N 期播客与 M 篇官方博客"，避免读者误判。
**来源任务**：automation-1777275206372 / 2026-09-16 AI Builders Digest
**标签**：#digest #去重 #feed #数据质量

## 2026-09-18 · 卡片数校验必须限定在 X section（blog 卡复用同一 class）
**场景**：生成后按 09-16 的校验清单跑「`grep -c '<article class="builder">'` == stats 的 Builders」。
**踩坑**：OFFICIAL BLOGS 段落的卡片**复用同一个 `article class="builder"`**，全文件 grep 得到 17（16 张 X 卡 + 1 张 blog 卡），与 stats 的 16 Builders 对不上。
**正确写法**：用 awk 限定区间只数 X section：
```bash
awk '/X \/ TWITTER/,/TAKEAWAY/' file | grep -c '<article class="builder">'
```
**配套结论**：更稳的做法是保留两个计数——全文件卡片数（用于确认 blog 卡渲染成功）与 X section 卡片数（用于对齐 Builders stats），不要只报一个数字。
**来源任务**：automation-1777275206372 / 2026-09-18 AI Builders Digest
**标签**：#digest #校验 #grep #stats口径

## 2026-09-18 · "0 podcast + 有 blog" 的版式组合（首次出现）
**场景**：feed 返回 podcast 数组为空，但 blog 有 1 篇（首收）。
**做法**：两个既有规则同时生效，互不冲突——
1. podcast = 0 → Takeaway 提为独立 `<section class="section">`，`section-meta` 写 "No Podcast Today"（09-10 规则）
2. blog 有正文 → OFFICIAL BLOGS 段落照常渲染完整卡片（09-06 规则）
3. 无需「已收录 / Already Covered」区块（无去重条目）
**顺序**：X/Twitter → Takeaway（独立）→ Official Blogs → footer。Takeaway 放在 blog 之前，因为它归纳的是 X 侧主线。
**来源任务**：automation-1777275206372 / 2026-09-18 AI Builders Digest
**标签**：#digest #模板 #版式分支

## 2026-09-19 · 主线跨日连续时，takeaway 要显式回指上一期

**场景**：今日主线（Anthropic Projects：组织单位从 session → project）是昨日主线（chat + Cowork 合并为一个 Claude：界面收敛）的同一家公司、连续两天的第二步动作。
**做法**：takeaway 里用一句把两期串起来——"Yesterday's news was Anthropic collapsing chat and Cowork into one surface; today's is the unit of organisation moving from the session to the project." 中文版同样回指。
**理由**：digest 是按日切片发布的，读者看到的每期是孤立页面；不回指的话，连续两天的同一个公司动作会读成两次独立发布，丢掉"这是一条演进线"这层信息。
**触发条件**：同一主体（公司/产品）连续两日出现在 feed 且动作有先后因果 → 显式回指；只是同一主题不同人发声 → 不必。
**来源任务**：automation-1777275206372 / 2026-09-19 AI Builders Digest
**标签**：#digest #编辑判断 #takeaway #跨期连续性

## 2026-09-20 · 有播客时也可以再加一个「日级 Takeaway」section

**场景**：当天 X 主线与播客讲的是同一个论点（本期：推理经济学——产品端 Jev 的单位成本，架构端扩散 LLM 的并行推理）。
**问题**：既有规则是「有 podcast → takeaway 嵌回播客卡内」。但卡内 takeaway 只能归纳播客，X 侧那条同样重要的主线就没有落脚点；若只写卡内 takeaway，读者会以为当天重点是播客。
**做法**：两者并存——播客卡内保留 takeaway（归纳播客，规则不变），**另加一个独立 `<section class="section">` 承载日级 Takeaway**，`section-meta` 写 "X + Podcast"，顺序 X → Podcast → Takeaway → footer。
**触发条件**：X 与播客指向同一论点、且 X 侧有独立可归纳的主线 → 加日级 section；播客是当天唯一实质内容 → 只用卡内 takeaway（09-13/09-17 旧式）。
**代价**：页面变长（本期 48600 bytes）。若某期 X 与播客毫无关联，不要为了版式加第二层。
**来源任务**：automation-1777275206372 / 2026-09-20 AI Builders Digest
**标签**：#digest #模板 #版式分支 #takeaway

## 2026-09-20 · feed 同一产品在几天内从「发布」走到「采用」，要按序列回指

**场景**：Jev 在 09-18 那期是一条发布消息（Rauch「18x faster (p95)」、将进 Vercel AI Gateway、Shipper 的降温版「jev is just a JSON classifier」）；09-20 那期同一个名字带上了使用量（Rauch「adoption 的数据和坊间反馈都惊人」、Vercel 上免费、有人建了 Jevable 目录站、Levie 挂了一个 Box 演示）。
**做法**：takeaway 里把它当序列写——先说 18 号是什么状态，再说今天多了什么证据（使用量 + 社区目录 + 企业演示），而不是当成一条独立新消息。
**与 09-19 那条规则的关系**：09-19 定的是「同一主体连续两日 → 回指」；本期是「同一产品隔了一天但构成证据链 → 也回指」。判断标准不是间隔天数，而是**今天的内容能不能自己成立**——今天的 Jev 若不提 18 号，读者会误以为当天才发布。
**附带收获**：同一批评者的位置反转（Shipper 18 号泼冷水、20 号辩护同类演示）值得一句点出，是很好的编辑素材。
**来源任务**：automation-1777275206372 / 2026-09-20 AI Builders Digest
**标签**：#digest #编辑判断 #跨期连续性 #takeaway

## 2026-09-19 · 「0 podcast + 0 blog」是最简三段式，已三期跑通

**场景**：feed 只返回 X 内容（本期 15/25/0/0）。
**版式**：X/Twitter section → 独立 Takeaway section（`section-meta: "No Podcast Today"`）→ footer。不需要 OFFICIAL BLOGS 段落，也不需要「已收录 / Already Covered」区块（无去重条目时）。
**校验简化**：这类期次只需对齐两项（Builders 卡片数、Tweets 唯一 URL 数），Podcast/Blog 恒为 0，diff 校验自然通过。
**历史对照**：与 09-10、09-11 同为最简式；09-18 是"0 podcast + 1 blog"变体（多一个 Blogs 段落）。
**来源任务**：automation-1777275206372 / 2026-09-19 AI Builders Digest
**标签**：#digest #模板 #版式分支 #校验
